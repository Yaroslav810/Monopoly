export enum MapFieldType {
    PROPERTY,
    CARDS,
    SPECIAL_ACTION,
    TAX,
    PRISON
}

export enum PropertyType {
    STREET,
    RAILWAY_STATION,
    MUNICIPAL_SERVICE
}

export enum CardsType {
    PUBLIC_TREASURE,
    CHANCE
}

export enum SpecialActionType {
    MOVING_TO,
    EMPTINESS,
    BEGINNING
}
